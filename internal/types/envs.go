package types

const (
	// DEBUG environment variable to control which logging namespaces are enabled.
	DEBUG string = "DEBUG"
	// DEBUG_LEVEL environment variable to control the log level.
	DEBUG_LEVEL string = "DEBUG_LEVEL"
	// DEBUG_COLOR environment variable to enable colored output.
	DEBUG_COLOR string = "DEBUG_COLOR"
)
